ブロックされているユーザーが新しく見つかりました。@ego_tter をフォローして、レポートを受け取りましょう。

フォローページ：
↓↓↓
<%= follow_url %>
↑↑↑

※ @ego_tter をフォロー後に上記のURLにアクセスしてください

あなたをブロックしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= total_count %>人<%= ' ※これから貯まります' if total_count == 0 %>

何をすればよい？：
1. @ego_tter をフォローした後に、上記のURLにアクセスする
2. その後に【ブロック通知 送信】とDMする

<% unless has_subscription %>
フォローしたくない場合は？：
有料プランのユーザーはフォローしていなくてもブロック通知の送信リクエストができます。
料金はこちら。無料のお試し期間あり <%= pricing_url %>
<% end %>

ブロック通知の停止：
【ブロック通知 停止】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

よくある質問 <%= support_url %>
問い合わせアカウント @egotter_cs

#egotter <%= '#hs' if has_subscription %>
