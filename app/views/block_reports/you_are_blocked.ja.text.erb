えごったーのブロック通知です。

あなたをブロックしている：
<%= blocked_names.join("\n") if blocked_names.any? %>
<%= remaining_users_count > 0 ? "さらに #{remaining_users_count}人" : "合計 #{users_count}人#{' ※これから貯まります' if users_count == 0}" %>

<% unless has_subscription %>
IDを全て見るには？：
有料プランのユーザーはIDを全て見ることができます。
料金はこちら。無料のお試し期間あり <%= pricing_url %>
<% end %>

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

ブロックの全データ：
<%= blockers_url %>

定期送信：
<%= stop_requested ? '無効' : '有効' %>

通知の停止：
【ブロック通知 停止】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

通知の設定変更 <%= settings_url %>
よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter <%= '#hs' if has_subscription %>
