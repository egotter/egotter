えごったーのブロック通知です。

あなたをブロックしている：
<%= block_urls.empty? ? '0人 ※これから貯まります' : block_urls.join("\n") %>

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

ブロックの全データ：
<%= blockers_url %>

定期送信：
<%= stop_requested ? '無効' : '有効' %>

通知の停止：
【ブロック通知 停止】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

通知の設定変更 <%= settings_url %>
よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter
