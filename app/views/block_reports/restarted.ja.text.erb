@<%= screen_name %> のブロック通知を再開します。

定期送信：
有効

ブロック通知の停止：
【ブロック通知 停止】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

あなたをブロックしている：
<%= profile_urls.empty? ? '0人 ※これから貯まります' : profile_urls.join("\n") %>

ブロックの全データ：
<%= blockers_url %>

#egotter
