@<%= screen_name %> のブロック通知を再開します。

定期送信：
有効
※ 有効になっている間は、ブロックしている人が新しく見つかると自動的に通知が届きます。

通知の停止：
【ブロック通知 停止】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

あなたをブロックしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= total_count %>人<%= ' ※これから貯まります' if total_count == 0 %>

ブロックの全データ：
<%= blockers_url %>

#egotter <%= '#hs' if has_subscription %>
