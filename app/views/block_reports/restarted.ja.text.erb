@<%= screen_name %> のブロック通知を再開します。

ブロック通知の停止：
【ブロック通知 停止】をDMで送信

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

最新の「ブロックされている」：
<%= profile_urls.empty? ? '0人' : profile_urls.join("\n") %>

「ブロックされている」の全データ：
<%= blockers_url %>

#egotter
