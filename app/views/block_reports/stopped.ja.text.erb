ブロック通知の定期配信は停止中です。

定期送信ステータス：
無効
※ 無効になっている間は、ブロックしている人が新しく見つかっても通知が届きません。

通知の再開：
【ブロック通知 再開】をDMで送信

通知の使い方：
【ブロック通知 使い方】をDMで送信

他の通知も停止する：
全ての通知を止めるには【全ての通知 停止】をDMにコピペして送ってください

ブロック通知とは：
@<%= screen_name %> をブロックしている人が新しく見つかった時に送信されるDMのことです。

あなたをブロックしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= total_count %>人<%= ' ※これから貯まります' if total_count == 0 %>

ブロックの全データ：
<%= blockers_url %>

#egotter <%= '#hs' if has_subscription %>
