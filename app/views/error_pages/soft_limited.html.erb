<div class="my-3"><%= t('.message_html', user: @screen_name, url: sign_in_path(via: current_via)) %></div>

<%= render(partial: 'profiles/profile_section', locals: {user: @user}) if @user %>

<%= render partial: 'ad_section', locals: {screen_name: @screen_name} %>

<%= render template: 'messages/search_limitation_soft_limited' %>
