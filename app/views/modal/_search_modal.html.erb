<%= modal_dialog id: 'search-modal', title: t('.title'), data: {url: search_histories_path}, size: 'modal-lg' do %>
  <%= render partial: 'shared/search_form', locals: {via: current_via('search_modal')} %>
<% end %>

<script>
  $(function () {
    var message = '<%= t('.failed') %>';
    new SearchModal('search-modal', message);
  });
</script>
