<%= modal_dialog(id: modal_id, title: t('.title'), button: {positive: t('.yes'), negative: t('.no'), category: 'warning'}) do %>
  <%= t('.body_html', screen_name: current_user.screen_name) %>
<% end %>
<script>
  $(function () {
    new ResetCacheModal('<%= modal_id %>');
  });
</script>
