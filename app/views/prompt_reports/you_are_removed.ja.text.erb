新しく <%= new_unfollowers_size %> 人にリムられました。次の更新は <%= report_interval_in_words %>後 です。

集計期間：
<%= previous_created_at %> - <%= current_created_at %>

フォロワー数：<% if previous_twitter_user.followers_count == current_twitter_user.followers_count %>
<%= previous_twitter_user.followers_count %> 人のままです。
<% else %>
<%= previous_twitter_user.followers_count %> 人から <%= current_twitter_user.followers_count %> 人に<%= previous_twitter_user.followers_count < current_twitter_user.followers_count ? '増えて' : '減って' %>います。
<% end %>

最新リムられ：<% if current_unfollowers_size == 0 %>
集計が始まってからまだリムられていません。
<% else %>
<%= current_unfollower_names.map{|name| "#{name} #{generic_timeline_url.gsub('__SN__', name)}" }.join("\n") %>
<% end %>
<% if current_unfollowers_size == 0 %>
検索結果ページ：
<% else %>
リムられ一覧：
<% end %><%= timeline_url %>

最終アクセス：<% if last_access_at %>
<%= last_access_at %>
<% else %>
アクセスなし
<% end %>

通知の設定変更はこちら <%= settings_url %> #egotter ID-<%= request_id %>