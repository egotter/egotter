リムられ通知の送信テストです。エラーはありませんでした。次からリムられ通知が届きます。

エラー：
なし

最新のリムられ通知：<% if user.prompt_reports.any? %>
<%= I18n.l(user.prompt_reports.first.created_at.in_time_zone('Tokyo'), format: :prompt_report_short) %>
<% else %>
リムられ通知はこれから送信されます。
<% end %>

最新のデータ更新：<% if twitter_user %>
<%= I18n.l(twitter_user.created_at.in_time_zone('Tokyo'), format: :prompt_report_short) %>
<% else %>
データはこれから更新されます。
<% end %>

検索結果ページ：
<%= timeline_url %>

通知の設定変更はこちら <%= settings_url %> #egotter