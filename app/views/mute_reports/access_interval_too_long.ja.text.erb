ミュートされているユーザーが新しく見つかりました。えごったーにアクセスして、レポートを受け取りましょう。

えごったーWebサイト：
↓↓↓
<%= access_url %>
↑↑↑

※ 上記のURLにアクセスしてログインまで完了してください。

あなたをミュートしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

何をすればよい？：
1. 上記のURLにアクセスする
2. その後に【ミュート通知 送信】とDMする

<% unless has_subscription %>
アクセスが面倒な場合は？：
有料プランのユーザーはアクセスしていなくてもミュート通知の送信リクエストができます。
料金はこちら。無料のお試し期間あり <%= pricing_url %>
<% end %>

通知の停止：
【ミュート通知 停止】をDMで送信

通知の使い方：
【ミュート通知 使い方】をDMで送信

よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter <%= '#hs' if has_subscription %>
