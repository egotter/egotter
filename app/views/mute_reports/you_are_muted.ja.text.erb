えごったーのミュート通知です。

あなたをミュートしている：
<%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

ミュート通知とは：
@<%= screen_name %> をミュートしている人が新しく見つかった時に送信されるDMのことです。
IDは分からず、人数だけが通知されます。

@<%= screen_name %> の分析結果：
<%= timeline_url %>

通知の停止：
【ミュート通知 停止】をDMで送信

通知の使い方：
【ミュート通知 使い方】をDMで送信

通知の設定変更 <%= settings_url %>
よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter
