ミュート通知のどの操作をリクエストしますか？

送信：
【ミュート通知 送信】をDMで送信
↑ 通知を一度だけ送信します。

停止：
【ミュート通知 停止】をDMで送信
↑ 定期的なミュート通知を停止します。

再開：
【ミュート通知 再開】をDMで送信
↑ 定期的なミュート通知を再開します。

使い方：
【ミュート通知 使い方】をDMで送信
↑ 使い方を送信します。

ミュート通知とは：
@<%= screen_name %> をミュートしている人が新しく見つかった時に送信されるDMのことです。
IDは分からず、人数だけが通知されます。

あなたをミュートしている：
<%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

@<%= screen_name %> の分析結果：
<%= timeline_url %>

通知の設定変更 <%= settings_url %>
よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter
