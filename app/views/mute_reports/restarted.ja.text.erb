@<%= screen_name %> のミュート通知を再開します。

定期送信ステータス：
有効
※ 有効になっている間は、ミュートしている人が新しく見つかると自動的に通知が届きます。

通知の停止：
【ミュート通知 停止】をDMで送信

通知の使い方：
【ミュート通知 使い方】をDMで送信

ミュート通知とは：
@<%= screen_name %> をミュートしている人が新しく見つかった時に送信されるDMのことです。
IDは分からず、人数だけが通知されます。

あなたをミュートしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

@<%= screen_name %> の分析結果：
<%= timeline_url %>

#egotter <%= '#hs' if has_subscription %>
