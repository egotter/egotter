@<%= screen_name %> のミュート通知を停止しました。

定期送信：
無効
※ 無効になっている間は、ミュートしている人が新しく見つかっても通知が届きません。

通知の再開：
【ミュート通知 再開】をDMで送信

通知の使い方：
【ミュート通知 使い方】をDMで送信

ミュート通知とは：
@<%= screen_name %> をミュートしている人が新しく見つかった時に送信されるDMのことです。
IDは分からず、人数だけが通知されます。

あなたをミュートしている：
<%= "#{first_name} と" if first_name.present? %>合計 <%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

@<%= screen_name %> の分析結果：
<%= timeline_url %>

#egotter <%= '#hs' if has_subscription %>
