@<%= screen_name %> のミュート通知を停止しました。

ミュート通知の再開：
【ミュート通知 再開】をDMで送信

ミュート通知とは：
@<%= screen_name %> をミュートしている人が新しく見つかった時に送信されるDMのことです。
IDは分からず、人数だけが通知されます。

最新の「ミュートされている」：
<%= users_count %>人<%= ' ※これから貯まります' if users_count == 0 %>

@<%= screen_name %> の分析結果：
<%= timeline_url %>

#egotter
