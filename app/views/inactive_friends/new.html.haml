- title @title
- set_meta_tags description: t('inactive_friends.new.description')

%h3.h4.inactive-friends.text-center= t('inactive_friends.new.lead_001')
.inactive-friends.lead.text-center= t('inactive_friends.new.lead_002').html_safe
.inactive-friends.lead.text-center= t('inactive_friends.new.lead_003').html_safe

= render partial: 'common/search_form', locals: {via: build_via('top_input')}
= render(partial: 'adsense/side_by_side', locals: {vertical: :top})

- if user_signed_in?
  = render partial: 'common/search_button', locals: {screen_name: current_user.screen_name, via: build_via('top_button')}
- else
  = sign_in_with_twitter_link(inactive_friends_top_path, 'button', class: 'btn btn-info btn-block')

.row.inactive-friends.screenshot
  .col-xs-12.col-sm-6.text-center
    %img{alt: "#{t('searches.common.egotter')}", src: image_path('/screenshot_iphone_332x560.png'), height: '160px'}
  .col-xs-12.col-sm-6.text-center
    %img{alt: "#{t('searches.common.egotter')}", src: image_path('/screenshot_laptop_961x560.png'), height: '160px'}

.inactive-friends.separator

#public-tweets-wrapper.twitter.tweets.center-block
:javascript
  $(function () {
    Home.setPublicTweets('#{load_public_tweets_path}', '#public-tweets-wrapper');
  });

.inactive-friends.lead.in-top-page.text-center= t('inactive_friends.new.lead_004').html_safe