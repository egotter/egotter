<%= period_name %>のリムられ通知です。直近の <%= date_range %> にリムられはありませんでした。

集計期間：
<%= I18n.l(start_date.in_time_zone('Tokyo'), format: :periodic_report_short) %> - <%= I18n.l(end_date.in_time_zone('Tokyo'), format: :periodic_report_short) %>

<% if first_followers_count && last_followers_count %>フォロワー数：
  <%= first_followers_count %> - <%= last_followers_count %><% end %>

検索結果ページ：
<%= timeline_url %>

通知の継続：
リムられ通知が必要な方は【<%= I18n.t('quick_replies.prompt_reports.label1') %>】をDMにコピペして送ってください

今すぐ通知を受信する：
リムられ通知を今すぐ受信するには【<%= I18n.t('quick_replies.prompt_reports.label3') %>】をDMにコピペして送ってください

定期配信：
<%= regular_subscription ? '有効' : '無効' %>

通知ID：
ID-<%= request_id %>-<%= twitter_user_id %>

通知の設定変更はこちら #egotter <%= settings_url %>