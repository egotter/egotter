<%= period_name %>のリムられ通知です。直近の <%= date_range %> にリムられはありませんでした。

集計期間：
<%= aggregation_period %>

フォロワー数：
<%= followers_count_change %><%= ' ※リムられていなくてもフォロワー数が1〜2人変わることがあります' if followers_count_notice %>

最新のリムられ：
<%= total_unfollower_urls.join("\n") %>

検索結果ページ：
<%= timeline_url %>

通知の継続：
リムられ通知が必要な方は【<%= I18n.t('quick_replies.prompt_reports.label1') %>】をDMにコピペして送ってください

今すぐ通知を受信する：
リムられ通知を今すぐ受信するには【<%= I18n.t('quick_replies.prompt_reports.label3') %>】をDMにコピペして送ってください

定期配信：
<%= regular_subscription ? '有効' : '無効（再開する時は「リムられ通知 再開」とDMしてください）' %>

通知ID：
ID-<%= request_id_text %>

通知の設定変更はこちら #egotter <%= settings_url %>