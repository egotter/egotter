<%= removed_by %> からリムられました。直近の <%= date_range %>、<%= period_name %>のリムられ通知です。

集計期間：
<%= aggregation_period %>

フォロワー数：
<%= followers_count_change %>

最新のリムられ：
<%= unfollower_urls.join("\n") %>

新しいフォロワー：
<%= new_follower_urls.empty? ? 'この期間では 0人 です' : new_follower_urls.join("\n") %>

あなたの分析結果：
<%= timeline_url %>

通知の継続：
【<%= I18n.t('quick_replies.prompt_reports.label1') %>】をDMで送信

今すぐ通知を受信：
【<%= I18n.t('quick_replies.prompt_reports.label3') %>】をDMで送信

定期配信：
<%= regular_subscription ? '有効' : '無効 ※再開するには「リムられ通知 再開」をDMで送信' %>

通知ID：
ID-<%= request_id_text %>

通知の設定変更 <%= settings_url %>
よくある質問 <%= faq_url %>
問い合わせアカウント @egotter_cs

#egotter